# ============================================================
# requirements.txt  –  Underwater Image Enhancement
# Install with: pip install -r requirements.txt
#
# PyTorch (CUDA 12.1): uncomment the torch block below and
# comment out the plain `torch` line, OR install manually:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# ============================================================

# --- Core deep learning ---
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# --- Scientific computing ---
numpy>=1.24.0
scipy>=1.11.0

# --- Image processing ---
opencv-python>=4.8.0
Pillow>=10.0.0
scikit-image>=0.21.0

# --- Loss / metrics ---
kornia>=0.7.0

# --- Profiling ---
thop>=0.1.1

# --- MambaVision backbone (NVIDIA 2024 hybrid Mamba+Transformer) ---
# Uses the HuggingFace transformers backend (pure-PyTorch, Windows/CPU-compatible).
# Pretrained weights download automatically from nvidia/MambaVision-{T,S,B}-1K
# on HuggingFace Hub on first use.
transformers>=4.40.0
huggingface_hub>=0.20.0
einops>=0.7.0         # tensor rearrangements used by MambaVisionMixer

# --- Native Mamba selective scan CUDA kernel ---
# Linux + CUDA (Kaggle) only. Gives ~200x speedup over the Python loop fallback.
# Install BEFORE running train.py on Kaggle:  pip install mamba-ssm
# Skip on Windows — the Python chunked-scan fallback is used automatically.
# mamba-ssm>=1.2.0
